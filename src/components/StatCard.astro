---
import type { Stat } from '@/types';

interface Props {
  stat: Stat;
}

const { stat } = Astro.props;
---

<div class="text-center">
  <!-- Número grande y prominente -->
  <div class="text-5xl md:text-6xl font-bold text-text-primary mb-2">
    {stat.value}
  </div>
  
  <!-- Label descriptivo -->
  <div class="text-base text-text-secondary">
    {stat.label}
  </div>
</div>