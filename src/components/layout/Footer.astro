---
import { navItems, contactInfo, companyInfo } from '@/lib/data';
import { getCurrentYear } from '@/lib/utils';

const currentYear = getCurrentYear();

const footerLinks = {
  servicios: [
    { label: 'Asesoría Fiscal', href: '/servicio/asesoria-fiscal' },
    { label: 'Contabilidad General', href: '/servicio/contabilidad-general' },
    { label: 'Auditoría', href: '/servicio/auditoria' },
    { label: 'Consultoría Financiera', href: '/servicio/consultoria-financiera' },
  ],
  empresa: [
    { label: 'Sobre Nosotros', href: '/sobre-nosotros' },
    { label: 'Blog', href: '/blog' },
    { label: 'Contacto', href: '/contacto' },
    { label: 'Newsletter', href: '/newsletter' },
  ],
  legal: [
    { label: 'Información Legal', href: '/informacion-legal' },
    { label: 'Aviso de Privacidad', href: '/aviso-privacidad' },
  ],
};
---

<footer class="bg-background border-t border-border">
  <!-- Main Footer -->
  <div class="container-custom py-16 md:py-20">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12">
      <!-- Company Info -->
      <div class="lg:col-span-1">
        <div class="mb-4">
          <div class="text-lg font-semibold text-text-primary leading-tight mb-1">
            {companyInfo.name}
          </div>
          <div class="text-xs text-text-tertiary">
            {companyInfo.slogan}
          </div>
        </div>
        <p class="text-sm text-text-secondary leading-relaxed mb-6">
          {companyInfo.description}
        </p>
      </div>

      <!-- Services Links -->
      <div>
        <h3 class="text-sm font-semibold text-text-primary mb-4">Servicios</h3>
        <ul class="space-y-3">
          {footerLinks.servicios.map(link => (
            <li>
              <a
                href={link.href}
                class="text-sm text-text-secondary hover:text-text-primary transition-colors duration-fast"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Company Links -->
      <div>
        <h3 class="text-sm font-semibold text-text-primary mb-4">Empresa</h3>
        <ul class="space-y-3">
          {footerLinks.empresa.map(link => (
            <li>
              <a
                href={link.href}
                class="text-sm text-text-secondary hover:text-text-primary transition-colors duration-fast"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Contact Info -->
      <div>
        <h3 class="text-sm font-semibold text-text-primary mb-4">Contacto</h3>
        <ul class="space-y-3 text-sm text-text-secondary">
          <li class="flex items-start">
            <svg class="w-5 h-5 mr-2 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
            </svg>
            <span class="leading-relaxed">
              {contactInfo.address.street}<br/>
              {contactInfo.address.city}, {contactInfo.address.state}, {contactInfo.address.zipCode}
            </span>
          </li>
          <li class="flex items-center">
            <svg class="w-5 h-5 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
            </svg>
            <a href={`tel:${contactInfo.phone}`} class="hover:text-text-primary transition-colors duration-fast">
              {contactInfo.phone}
            </a>
          </li>
          <li class="flex items-center">
            <svg class="w-5 h-5 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            <a href={`mailto:${contactInfo.email}`} class="hover:text-text-primary transition-colors duration-fast">
              {contactInfo.email}
            </a>
          </li>
        </ul>
      </div>
    </div>
  </div>

  <!-- Bottom Footer -->
  <div class="border-t border-border">
    <div class="container-custom py-6">
      <div class="flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0">
        <p class="text-sm text-text-tertiary text-center md:text-left">
          © {currentYear} {companyInfo.legalName}. Todos los derechos reservados.
        </p>
        <div class="flex flex-wrap justify-center md:justify-end gap-6">
          {footerLinks.legal.map(link => (
            <a
              href={link.href}
              class="text-sm text-text-tertiary hover:text-text-primary transition-colors duration-fast"
            >
              {link.label}
            </a>
          ))}
        </div>
      </div>
    </div>
  </div>
</footer>